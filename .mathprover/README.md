# Local MathProver runtime (generated — do not commit secrets)

Copy `meta.json.example` to `meta.json` for optional UI overlay (foundations, paper blocks).

Regenerate graph:

```bash
python3 ../scripts/reindex_graph.py
```

See `docs/mathprover.md` in the repo root.
