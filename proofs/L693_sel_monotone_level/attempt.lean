-- WORKER SCRATCHPAD for L693 sel_monotone_level.
-- 1. Rewrite μ_sel(A_ge j) via coea_sel_measure_cdf (L669).
-- 2. Prove (1-p)^λ ≤ 1-p for p ∈ [0,1], λ ≥ 1, via pow_le_pow_right_of_le_one.
-- 3. Subtract to get 1 - (1-p)^λ ≥ p.
-- 4. Cast toReal.

import LBTCoupling

-- TODO(worker): proof body.
